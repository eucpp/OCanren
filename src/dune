(include_subdirs unqualified)

(library
  (name ocanren)
  (libraries GT GT.syntax.all)
  (flags (:standard -w -9-32-27-39-7-8-33-34-26-50 -rectypes))
  (preprocess
    (action (run camlp5 %{read-lines:../config/camlp5-flags} %{input-file}))
  )
)

