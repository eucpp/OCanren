(executable
  (name discover)
  (modules Discover)
  (libraries dune.configurator)
)

(executable
  (name dunegen)
  (modules Dunegen)
  (libraries str)
)

(rule
  (targets camlp5-flags gt-flags logger-flags)
  (action (run ./discover.exe))
)

(rule
  (targets dune.tests.log.gen dune.tests.diff.gen)
  (deps dune.tests.log.tpl dune.tests.diff.tpl)
  (mode (promote-into ../regression))
  (action (run ./dunegen.exe))
)
